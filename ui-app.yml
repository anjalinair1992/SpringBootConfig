math:
 service:
  base:
   url: http://localhost:7000
  square:
   url: /sq
  cube:
   url: /cu 

server:
  port: 8080
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   
#    enabled: false

#-Deureka.client.enabled=false

mathdb:
 service:
  #url: http://localhost:8082/mathdb 
   url: http://localhost:7000/db
